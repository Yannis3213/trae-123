import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div class="text-center py-12">
      <h1 class="text-2xl font-bold text-gray-900 mb-4">消缺验收</h1>
      <p class="text-gray-500">模块建设中...</p>
    </div>
  );
});
