# 公关传播团队 - 月底集中处理传播计划单系统

## 端口说明

| 服务 | 端口 | 地址 |
|------|------|------|
| 前端 (SolidStart) | 3107 | http://localhost:3107 |
| 后端 (NestJS) | 8107 | http://localhost:8107 |
| CORS 白名单 | - | http://localhost:3107 |

## 快速启动

### 1. 启动后端

```bash
cd backend
npm install
npm run dev
```

后端启动后会自动创建 SQLite 数据库文件 `pr_system.db`。

### 2. 初始化演示数据

后端启动后，调用种子接口：

```bash
curl -X POST http://localhost:8107/api/plans/seed
```

或者在前端列表页点击「初始化演示数据」按钮。

### 3. 启动前端

```bash
cd frontend
npm install
PORT=3107 npm run dev
```

访问 http://localhost:3107

## SQLite 数据库

- 数据库文件：`backend/pr_system.db`
- 首次启动时由 TypeORM 自动创建表结构（`synchronize: true`）
- 删除 `pr_system.db` 后重启后端可重建数据库
- 重建后需重新调用 `/api/plans/seed` 初始化演示数据

## 演示账号

| 账号 | 角色 | 姓名 | 对应岗位 |
|------|------|------|----------|
| registrar | 传播计划登记员 | 张晓明 | 客户经理 |
| reviewer | 传播计划审核主管 | 李审核 | 内容审核 |
| director | 公关传播团队复核负责人 | 王总监 | 项目总监 |

登录页选择任意演示账号即可进入系统，登录后可在顶部角色栏一键切换角色。

## 四类可试异常的单据

| 编号 | 标题 | 异常类型 | 试法 |
|------|------|----------|------|
| PR-2026-1001 | Q2品牌传播计划-A客户 | 正常流转 | 切换为审核主管签收→审核通过→切换项目总监复核归档 |
| PR-2026-1004 | 缺材料传播计划-B客户 | 缺材料 | 切换项目总监点复核归档，弹出资料缺失拦截提示 |
| PR-2026-1005 | 逾期未处理-线下活动传播 | 超时/逾期 | 列表中可见红色逾期标识，批量复核时被逐条拦截 |
| PR-2026-1007 | 退回补正-C客户投放确认 | 退回补正 | 切换登记员点补正提交，可重新进入流转 |
| PR-2026-1008 | 异常回传-D客户传播计划 | 状态冲突/异常回传 | 切换登记员补正后重新流转，模拟状态冲突场景 |

## 核心功能

- **角色驱动**：三个角色各司其职，操作按钮按角色动态显示
- **状态流转**：待签收 → 审核中 → 待复核 → 已归档，异常分支：退回补正、异常回传
- **复核拦截**：资料缺失、状态冲突、越权推进、超期未处理四类拦截
- **批量处理**：逐条校验，逾期不可整批放行，失败记录留审计轨迹
- **到期预警**：正常(绿)、临期(橙)、逾期(红)，逾期显示责任人
- **审计轨迹**：每次操作留记录，含操作人、时间、状态变更、退回原因
- **版本控制**：乐观锁机制，防止旧版本重复提交
