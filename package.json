{
  "name": "supervision-side-record-system",
  "version": "1.0.0",
  "description": "工程监理公司-月底集中处理旁站记录单系统",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "init:db": "npm --prefix backend run init:db",
    "seed": "npm --prefix backend run seed",
    "start:backend": "npm --prefix backend run start",
    "start:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
